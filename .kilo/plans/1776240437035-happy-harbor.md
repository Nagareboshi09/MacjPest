# Plan: Add Philippine Holidays Calendar to Admin Dashboard

## Objective
Integrate a real-time calendar into `Admin Side/dashboard.php` that displays Philippine holidays for the current year, fetched from a public API. The calendar should use FullCalendar library and highlight holidays in red.

## Requirements
- FullCalendar library (via CDN)
- Tallyfy National Holidays API (free, no key required)
- Current year determination (via PHP `date('Y')`)
- Responsive design matching existing dashboard styles

## Steps
1. **Update HTML Head**: Add FullCalendar CSS and JS CDN links to `<head>` section.
2. **Add Calendar HTML**: Insert new dashboard row with calendar div after existing chemical inventory cards.
3. **Implement JavaScript**: Add script to fetch holidays from API and initialize FullCalendar with holiday events.
4. **Add CSS**: Include responsive styles for calendar container.
5. **Test Integration**: Verify calendar loads, displays holidays correctly, and handles API errors.

## Files to Modify
- `Admin Side/dashboard.php`: Add HTML, CSS, JS as specified in previous response.

## Potential Issues
- API downtime: Fallback message displayed.
- CORS or network issues: Calendar shows error message.
- Mobile responsiveness: Test on different screen sizes.

## Resources
- FullCalendar docs: https://fullcalendar.io/docs
- Tallyfy API: https://tallyfy.com/national-holidays/